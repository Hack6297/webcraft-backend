WebCraft Fullstack Project

Backend:
- Django + DRF + JWT + Channels
- Use Dockerfile to deploy on Render

Frontend:
- React + Axios + Socket.IO
- Deploy via Vercel with `npm run build` and `build` output

No YAML needed. Set environment variables via Render/Vercel UI.